/**
 * @deprecated Use @/libs/weapon/playwright directly.
 * Re-exports all browser control from the playwright weapon.
 */
export { executeSteps, initBrowser, captureAuth, readState, checkCredentials, STATE_FILE } from "@/libs/weapon/playwright";
