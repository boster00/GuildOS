/**
 * Adventurer runtime — execution scope only (`advance.js`). Manage-plane hooks: `manage.js`.
 * Roster, UI registry, and server helpers live under `@/libs/proving_grounds`.
 */
export * from "./advance.js";
