let me tell a story of how the runesmith created a skill book "BigQuery" to adventurer "BigQuery Expert" uses it to fetch most recent 20 events in BigQuery Database. Help interpret the process into a workflow, which I will develop step by step. 

The point of the story is to enable this development to checking loop that allows semi autonomouse self expansion. 

In the begining, guildmaster receives a client who said "I need help fetching 20 most recent events from BigQuery" (natural language input) 

The request was passed to the questmaster, the cat, who thought (the triaging process): 
1. I checked all my adventurers, no one has that capability (initial triaging query, checking NL against roster of agents), we must recruit a new adventurer who can do BigQuery. This is the most immediate goal, so I should add a note about next steps after the goal is accomplished. 
Key concepts: first step in advancing the idea stage is to evalaute if we have the right adventurer for the job. If so, proceed to the planning stage, if not, proceed to the recruiting workflow by naming the quest as "recruiting an adventurer for XXX" and put the user instruction into the what to do next description of the quest. These "next steps" instructions should be recorded in the description of the quest at the end under the label "next steps" and in a number bullet format like 1. xxxx 2. xxxx. At the closing step of these quests, the next steps quest will be used to create new quests, which will use item 1 as main quest and inherit the next steps. These AI actions prompt template are defined in questmaster skill book actions. 
2. the quest is now "Recruit an adventurer for XXX" and assigned to the guildmaster, with description of how to do it generated by step 1, and with a note of the original user request at the end of the description. In the planning stage, pig (the guildmaster) sees the quest and thought (the planning process specific for creating adventurer) "I looked through all the skill books and there is nothing related to bigquery, I must consult the RuneSmith to create a skill book first. 
Key concepts: check all existing skill books and decide if we need to create a new one. If so, change the main quest to develop a new skill book for xxxx, and add descriptions on what needs to be created (AI generate a general description of a workflow/actions in the skill book) and adjust the next steps to reflect the current quest, which is recruit an advneturer, then get 20 events. 
These dependency checks go into a loop until AI decides that the action is achievable with current capabiltiies. 
These instructions live in database under each adventurer's system_prompt
3. the quest is now "create a skill book for big query". The Runesmith (once the pig decides that a skill book is the next step, the quest changes ownership to the RuneSmith) thought: this involves an external data source and I need a weapon to connect with that. I must pass this onto the blacksmith. 
Key concepts: each agent has a strict and narrow set of criteria about what to do in each situation, this allows a scalable decision tree and is easy to mentally map what should happen at each situation by mimicing character interactions. The quest gets passed around and each time logging the current objective into the first slot of the next steps (since this is such a core mechanism, maybe we should create a column for this, talk to me about this decision)
4. the weapons smith saw the quest and thought, I can do that, and goes into execution mode. Weapon smith has a skill book and a weapon called "cursorCLI" which can issue commands to cursor. Weapon smith has a clearly documented system prompt about how to command the agent (cursor CLI) and a lot of guild rails are baked into the cursorCLI weapon actions. (talk to me about creating this weapon manually). The blacksmith created a blue print about what actions to take, and used the weapon cursorCLI to change the proving grounds UI to outline the steps for creating and testing a new weapon. The general approach is this: he reads the ultimate goal, which is to grab some events, he gives this command through the cursorCLI weapon (action: updateProvingGrounds) "replace the proving grounds steps and testing UI, show a list of steps to perform to set up the weapon to access big query, and then update the testskillbook action "test" to call the weapon "bigQuery" action "test". The steps instruction to the user should be: step 1: a detailed description of where to obtain the connection credentials for big query 2. how to input the credentials to allow auth. Depending on research this will be either for user to enter the credentials in the formulary, or to build a specific api/auth/name of application webhook to listen for auth ping backs and store the auth token in the potions shelf (database) 3. once auth is done, run the test action by calling the "test" action of the testskillbook. 

Summarize the above story, clearly layout the parts where agentic instructions need to be given. Clearly layout the parts I need to manually create, and which file/folder those code will live. 
